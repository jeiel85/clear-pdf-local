# Local build secrets, read by the secrets Gradle plugin.
# open-local-scan ships NO API keys and makes NO network calls, so none are required.
# (Release signing reads KEYSTORE_PATH / STORE_PASSWORD / KEY_PASSWORD from environment
#  variables, not from this file — see README.)
